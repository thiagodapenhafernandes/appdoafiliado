<!DOCTYPE html>
<html>
  <head>
    <title>Link Flow - Painel Administrativo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <!-- Logo -->
            <div class="flex-shrink-0 flex items-center">
              <%= link_to admin_dashboard_index_path, class: "text-xl font-bold text-indigo-600" do %>
                🔗 Link Flow Admin
              <% end %>
            </div>
            
            <!-- Navigation Links -->
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8 items-center">
              <%= admin_navigation_link "Dashboard", admin_dashboard_index_path %>
              <%= admin_navigation_link "Usuários", admin_users_path %>
              <%= admin_navigation_link "Planos", admin_plans_path %>
              
              <% if current_user&.super_admin? %>
                <%= admin_navigation_link "Configurar Stripe", admin_stripe_config_index_path %>
                <%= admin_navigation_link "Shopee Config", admin_shopee_configs_path %>
              <% end %>
            </div>
          </div>
          
          <!-- Right side -->
          <div class="flex items-center space-x-4">
            <%= link_to "Voltar ao App", root_path, 
                class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium" %>
            
            <div class="relative">
              <div class="flex items-center space-x-2 text-sm">
                <span class="text-gray-700"><%= current_user.full_name %></span>
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-indigo-100 text-indigo-800">
                  <%= current_user.role.humanize %>
                </span>
              </div>
            </div>
            
            <%= button_to destroy_user_session_path,
                method: :delete,
                class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium bg-transparent border-0 cursor-pointer",
                form: { class: "inline" } do %>
              Sair
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <div class="px-4 py-6 sm:px-0">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
