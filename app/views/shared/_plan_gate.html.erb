<%# Lightweight inline gate for plan-specific features %>
<%
  title ||= "Disponível no Plano Completo"
  message ||= "Assine o Plano Completo de R$ 47/mês para desbloquear esta funcionalidade."
  cta_label ||= "Conhecer o Plano Completo"
  cta_path ||= plans_path
  plan_badge ||= nil
  plan_icon ||= 'fas fa-crown'
%>
<div class="rounded-lg border border-dashed border-orange-300 bg-orange-50/70 p-4">
  <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
    <div class="flex items-start gap-3">
      <div class="mt-1 hidden sm:flex w-9 h-9 bg-orange-100 text-orange-500 rounded-full items-center justify-center">
        <i class="<%= plan_icon %> text-sm"></i>
      </div>
      <div class="text-center sm:text-left">
        <div class="flex flex-col sm:flex-row sm:items-center gap-2 justify-center sm:justify-start">
          <p class="text-sm font-semibold text-orange-700"><%= title %></p>
          <% if plan_badge.present? %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-orange-100 text-orange-700">
              <%= plan_badge %>
            </span>
          <% end %>
        </div>
        <p class="text-sm text-orange-600 mt-1 leading-relaxed"><%= message %></p>
      </div>
    </div>
    <%= link_to cta_path, class: "inline-flex items-center gap-2 px-4 py-2 rounded-md bg-orange-500 hover:bg-orange-600 text-white text-sm font-medium transition-colors self-center sm:self-auto" do %>
      <i class="fas fa-arrow-up-right-from-square text-xs"></i>
      <span><%= cta_label %></span>
    <% end %>
  </div>
</div>
