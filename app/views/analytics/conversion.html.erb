<% content_for :title, "Análise de Conversão" %>

<% if @advanced_analytics_locked %>
  <div class="space-y-6">
    <%= render 'shared/plan_gate',
        title: 'Funil de conversão disponível no Plano Completo',
        message: 'Visualize cada etapa do funil, calcule ROI real e atribuição de campanhas de tráfego com relatórios avançados.',
        cta_label: 'Ver Plano Completo' %>
    <div class="bg-white border border-gray-200 rounded-2xl p-6 shadow-sm">
      <h2 class="text-lg font-semibold text-gray-900 mb-3">Benefícios do upgrade</h2>
      <ul class="list-disc list-inside text-sm text-gray-600 space-y-1">
        <li>Funil completo do clique à compra com taxa de conversão por etapa</li>
        <li>Análise de ROI considerando investimento em tráfego por subID</li>
        <li>Atribuição de vendas para entender quais anúncios geram lucro</li>
      </ul>
    </div>
  </div>
<% else %>

<div class="space-y-6">
  <!-- Header -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">🎯 Análise de Conversão</h1>
      <p class="text-gray-600 mt-1">Funil de conversão, ROI e análise de atribuição avançada</p>
    </div>
    
    <div class="flex space-x-3">
      <%= link_to analytics_path, class: "bg-gray-100 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-200 transition-colors" do %>
        ← Voltar ao Analytics
      <% end %>
      
      <% if current_user.can_export_pdf? %>
        <%= link_to analytics_export_pdf_path(format: :pdf), 
            class: "bg-orange-500 text-white px-4 py-2 rounded-lg hover:bg-orange-600 transition-colors flex items-center",
            target: "_blank" do %>
          📄 Exportar PDF
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- Funil de Conversão -->
  <div class="bg-white rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-lg font-semibold text-gray-900">🔄 Funil de Conversão</h2>
      <p class="text-sm text-gray-600">Visualize o caminho do cliente desde o clique até a compra</p>
    </div>
    <div class="p-6">
      <% if @conversion_funnel.present? %>
        <div class="space-y-6">
          <!-- Etapas do Funil -->
          <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <% @conversion_funnel.each_with_index do |stage, index| %>
              <div class="relative">
                <!-- Seta de conexão -->
                <% if index < @conversion_funnel.length - 1 %>
                  <div class="hidden md:block absolute top-1/2 -right-2 transform -translate-y-1/2 z-10">
                    <svg class="w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                    </svg>
                  </div>
                <% end %>
                
                <!-- Card da Etapa -->
                <div class="bg-gradient-to-br from-blue-50 to-indigo-100 rounded-lg p-4 border-2 border-blue-200 relative">
                  <div class="text-center">
                    <div class="text-2xl mb-2"><%= stage[:icon] %></div>
                    <h3 class="font-semibold text-gray-900 text-sm"><%= stage[:name] %></h3>
                    <div class="text-2xl font-bold text-blue-600 mt-2"><%= number_with_delimiter(stage[:count]) %></div>
                    <div class="text-xs text-gray-600 mt-1"><%= number_with_precision(stage[:percentage], precision: 1) %>%</div>
                  </div>
                  
                  <!-- Taxa de Conversão -->
                  <% if index > 0 %>
                    <div class="mt-3 pt-3 border-t border-blue-200">
                      <div class="flex justify-between items-center text-xs">
                        <span class="text-gray-600">Taxa de conversão:</span>
                        <span class="font-medium text-green-600">
                          <%= number_with_precision(stage[:conversion_rate], precision: 1) %>%
                        </span>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>

          <!-- Insights do Funil -->
          <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
            <h4 class="font-semibold text-yellow-800 mb-2">💡 Insights do Funil</h4>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
              <div>
                <p class="text-yellow-700">
                  <strong>Taxa de conversão geral:</strong> 
                  <%= number_with_precision(@conversion_funnel.last[:percentage], precision: 1) %>%
                </p>
              </div>
              <div>
                <p class="text-yellow-700">
                  <strong>Maior gargalo:</strong> 
                  Entre <%= @conversion_funnel[0][:name] %> e <%= @conversion_funnel[1][:name] %>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% else %>
        <div class="text-center py-8">
          <div class="text-gray-400 text-4xl mb-4">🔄</div>
          <p class="text-gray-600">Dados insuficientes para análise do funil de conversão.</p>
          <p class="text-sm text-gray-500 mt-2">Importe mais dados de comissão e cliques para esta análise.</p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Análise de ROI -->
  <div class="bg-white rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-lg font-semibold text-gray-900">💰 Análise de ROI</h2>
      <p class="text-sm text-gray-600">Retorno sobre investimento por canal e campanha</p>
    </div>
    <div class="p-6">
      <% if @roi_analysis.present? %>
        <div class="space-y-6">
          <!-- ROI Geral -->
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div class="bg-gradient-to-r from-green-100 to-emerald-100 rounded-lg p-4 border border-green-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm text-green-600 font-medium">ROI Geral</p>
                  <p class="text-2xl font-bold text-green-700">
                    <%= @roi_analysis[:overall_roi] > 0 ? '+' : '' %><%= number_with_precision(@roi_analysis[:overall_roi], precision: 1) %>%
                  </p>
                </div>
                <div class="text-3xl">💹</div>
              </div>
            </div>
            
            <div class="bg-gradient-to-r from-blue-100 to-cyan-100 rounded-lg p-4 border border-blue-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm text-blue-600 font-medium">Investido</p>
                  <p class="text-2xl font-bold text-blue-700">
                    R$ <%= number_with_precision(@roi_analysis[:total_invested], precision: 2, delimiter: '.', separator: ',') %>
                  </p>
                </div>
                <div class="text-3xl">💸</div>
              </div>
            </div>
            
            <div class="bg-gradient-to-r from-purple-100 to-violet-100 rounded-lg p-4 border border-purple-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm text-purple-600 font-medium">Retorno</p>
                  <p class="text-2xl font-bold text-purple-700">
                    R$ <%= number_with_precision(@roi_analysis[:total_return], precision: 2, delimiter: '.', separator: ',') %>
                  </p>
                </div>
                <div class="text-3xl">💰</div>
              </div>
            </div>
          </div>

          <!-- ROI por Canal -->
          <div>
            <h3 class="text-lg font-semibold text-gray-900 mb-4">ROI por Canal</h3>
            <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Canal</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Investido</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Retorno</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ROI</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                  </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                  <% @roi_analysis[:by_channel].each do |channel_roi| %>
                    <tr class="hover:bg-gray-50">
                      <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                          <span class="text-2xl mr-2"><%= channel_roi[:icon] %></span>
                          <div>
                            <div class="text-sm font-medium text-gray-900"><%= channel_roi[:channel] %></div>
                            <div class="text-sm text-gray-500"><%= channel_roi[:campaigns] %> campanhas</div>
                          </div>
                        </div>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                        R$ <%= number_with_precision(channel_roi[:invested], precision: 2, delimiter: '.', separator: ',') %>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-green-600">
                        R$ <%= number_with_precision(channel_roi[:return], precision: 2, delimiter: '.', separator: ',') %>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap">
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium 
                                    <%= channel_roi[:roi] > 100 ? 'bg-green-100 text-green-800' : 
                                        channel_roi[:roi] > 0 ? 'bg-yellow-100 text-yellow-800' : 
                                        'bg-red-100 text-red-800' %>">
                          <%= channel_roi[:roi] > 0 ? '+' : '' %><%= number_with_precision(channel_roi[:roi], precision: 1) %>%
                        </span>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap">
                        <% if channel_roi[:roi] > 200 %>
                          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                            🚀 Excelente
                          </span>
                        <% elsif channel_roi[:roi] > 100 %>
                          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                            📈 Bom
                          </span>
                        <% elsif channel_roi[:roi] > 0 %>
                          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                            ⚠️ Atenção
                          </span>
                        <% else %>
                          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                            🔴 Prejuízo
                          </span>
                        <% end %>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      <% else %>
        <div class="text-center py-8">
          <div class="text-gray-400 text-4xl mb-4">💰</div>
          <p class="text-gray-600">Dados insuficientes para análise de ROI.</p>
          <p class="text-sm text-gray-500 mt-2">Adicione dados de investimento em anúncios para esta análise.</p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Análise de Atribuição -->
  <div class="bg-white rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-lg font-semibold text-gray-900">🎭 Análise de Atribuição</h2>
      <p class="text-sm text-gray-600">Como diferentes canais contribuem para as conversões</p>
    </div>
    <div class="p-6">
      <% if @attribution_analysis.present? %>
        <div class="space-y-6">
          <!-- Modelos de Atribuição -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- First Click -->
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
              <h3 class="font-semibold text-blue-900 mb-3">🥇 Primeiro Clique</h3>
              <p class="text-sm text-blue-700 mb-4">Dá crédito total ao primeiro canal que trouxe o cliente</p>
              <div class="space-y-2">
                <% @attribution_analysis[:first_click].each do |channel| %>
                  <div class="flex justify-between items-center">
                    <span class="text-sm font-medium"><%= channel[:channel] %></span>
                    <div class="flex items-center">
                      <span class="text-sm text-blue-600 mr-2"><%= number_with_precision(channel[:percentage], precision: 1) %>%</span>
                      <div class="w-16 bg-blue-200 rounded-full h-2">
                        <div class="bg-blue-600 h-2 rounded-full" style="width: <%= channel[:percentage] %>%"></div>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>

            <!-- Last Click -->
            <div class="bg-green-50 border border-green-200 rounded-lg p-4">
              <h3 class="font-semibold text-green-900 mb-3">🏁 Último Clique</h3>
              <p class="text-sm text-green-700 mb-4">Dá crédito total ao último canal antes da conversão</p>
              <div class="space-y-2">
                <% @attribution_analysis[:last_click].each do |channel| %>
                  <div class="flex justify-between items-center">
                    <span class="text-sm font-medium"><%= channel[:channel] %></span>
                    <div class="flex items-center">
                      <span class="text-sm text-green-600 mr-2"><%= number_with_precision(channel[:percentage], precision: 1) %>%</span>
                      <div class="w-16 bg-green-200 rounded-full h-2">
                        <div class="bg-green-600 h-2 rounded-full" style="width: <%= channel[:percentage] %>%"></div>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>

          <!-- Jornada do Cliente -->
          <div>
            <h3 class="text-lg font-semibold text-gray-900 mb-4">🗺️ Jornadas Mais Comuns</h3>
            <div class="space-y-3">
              <% @attribution_analysis[:customer_journeys].each_with_index do |journey, index| %>
                <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                  <div class="flex items-center justify-between mb-2">
                    <span class="text-sm font-medium text-gray-900">Jornada #<%= index + 1 %></span>
                    <span class="text-sm text-gray-600"><%= journey[:count] %> conversões (<%= number_with_precision(journey[:percentage], precision: 1) %>%)</span>
                  </div>
                  <div class="flex items-center space-x-2">
                    <% journey[:path].each_with_index do |step, step_index| %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-white border border-gray-300">
                        <%= step %>
                      </span>
                      <% if step_index < journey[:path].length - 1 %>
                        <svg class="w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                        </svg>
                      <% end %>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          </div>

          <!-- Insights de Atribuição -->
          <div class="bg-purple-50 border border-purple-200 rounded-lg p-4">
            <h4 class="font-semibold text-purple-800 mb-2">🔍 Insights de Atribuição</h4>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
              <div>
                <p class="text-purple-700">
                  <strong>Canal de descoberta principal:</strong> 
                  <%= @attribution_analysis[:top_discovery_channel] %>
                </p>
              </div>
              <div>
                <p class="text-purple-700">
                  <strong>Canal de conversão principal:</strong> 
                  <%= @attribution_analysis[:top_conversion_channel] %>
                </p>
              </div>
              <div>
                <p class="text-purple-700">
                  <strong>Tempo médio até conversão:</strong> 
                  <%= @attribution_analysis[:avg_time_to_conversion] %> dias
                </p>
              </div>
              <div>
                <p class="text-purple-700">
                  <strong>Pontos de contato médios:</strong> 
                  <%= @attribution_analysis[:avg_touchpoints] %>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% else %>
        <div class="text-center py-8">
          <div class="text-gray-400 text-4xl mb-4">🎭</div>
          <p class="text-gray-600">Dados insuficientes para análise de atribuição.</p>
          <p class="text-sm text-gray-500 mt-2">Importe mais dados de cliques e conversões para esta análise.</p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Recomendações de Otimização -->
  <div class="bg-gradient-to-r from-orange-100 to-red-100 rounded-lg shadow border border-orange-200">
    <div class="px-6 py-4 border-b border-orange-200">
      <h2 class="text-lg font-semibold text-orange-900">🎯 Recomendações de Otimização</h2>
      <p class="text-sm text-orange-700">Ações sugeridas baseadas na análise de conversão</p>
    </div>
    <div class="p-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="space-y-3">
          <h3 class="font-semibold text-orange-900">📈 Para Melhorar Conversão:</h3>
          <ul class="space-y-2 text-sm text-orange-800">
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Foque nos canais com maior ROI (acima de 200%)
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Otimize o funil onde há maior perda de conversão
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Teste diferentes criativos nos canais de baixa performance
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Aumente investimento nos melhores SubIDs
            </li>
          </ul>
        </div>
        
        <div class="space-y-3">
          <h3 class="font-semibold text-orange-900">💰 Para Melhorar ROI:</h3>
          <ul class="space-y-2 text-sm text-orange-800">
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Reduza investimento em canais com ROI negativo
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Implemente retargeting nos canais de descoberta
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Analise horários de pico para cada canal
            </li>
            <li class="flex items-start">
              <span class="text-orange-600 mr-2">•</span>
              Teste combinações de canais na jornada do cliente
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
<% end %>

</div>
