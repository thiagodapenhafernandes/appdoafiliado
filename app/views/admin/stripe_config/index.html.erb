<div class="mb-8">
  <div class="flex justify-between items-center">
    <div>
      <h1 class="text-3xl font-bold text-gray-900">Configuração Stripe</h1>
      <p class="mt-2 text-gray-600">Configure a integração com o Stripe para pagamentos</p>
    </div>
    <div class="flex space-x-3">
      <%= link_to "Sincronizar Planos", sync_plans_admin_stripe_config_index_path, 
          data: { "turbo-method": :post },
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700" %>
    </div>
  </div>
</div>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
  <!-- Configuration Status -->
  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">Status da Configuração</h3>
      <div class="space-y-4">
        <div class="flex items-center justify-between">
          <span class="text-sm font-medium text-gray-500">Chave Pública</span>
          <% if @stripe_config[:publishable_key].present? %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
              Configurada
            </span>
          <% else %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
              Não Configurada
            </span>
          <% end %>
        </div>
        
        <div class="flex items-center justify-between">
          <span class="text-sm font-medium text-gray-500">Chave Secreta</span>
          <% if @stripe_config[:secret_key_present] %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
              Configurada
            </span>
          <% else %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
              Não Configurada
            </span>
          <% end %>
        </div>
        
        <div class="flex items-center justify-between">
          <span class="text-sm font-medium text-gray-500">Webhook Secret</span>
          <% if @stripe_config[:webhook_secret_present] %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
              Configurado
            </span>
          <% else %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
              Opcional
            </span>
          <% end %>
        </div>
      </div>
      
      <% if @stripe_config[:publishable_key].present? %>
        <div class="mt-4 p-3 bg-gray-50 rounded-md">
          <p class="text-xs text-gray-500">Chave Pública:</p>
          <code class="text-xs bg-white px-2 py-1 rounded"><%= @stripe_config[:publishable_key] %></code>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">Ações Rápidas</h3>
      <div class="space-y-3">
        <%= form_tag sync_plans_admin_stripe_config_index_path, method: :post, class: "w-full" do %>
          <%= submit_tag "Sincronizar Planos com Stripe",
              class: "w-full inline-flex justify-center items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700" %>
        <% end %>
        
        <% if @stripe_config[:webhook_secret_present] %>
          <%= form_tag test_webhook_admin_stripe_config_index_path, method: :post, class: "w-full" do %>
            <%= submit_tag "Testar Webhook",
                class: "w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
          <% end %>
        <% end %>
        
        <%= link_to "Gerenciar Planos", admin_plans_path,
            class: "w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
      </div>
    </div>
  </div>
</div>

<!-- Stripe Products -->
<% if @products.any? %>
  <div class="mt-8">
    <div class="bg-white shadow overflow-hidden sm:rounded-md">
      <div class="px-4 py-5 sm:px-6">
        <h3 class="text-lg leading-6 font-medium text-gray-900">Produtos no Stripe</h3>
        <p class="mt-1 max-w-2xl text-sm text-gray-500">Produtos ativos na sua conta Stripe</p>
      </div>
      <ul class="divide-y divide-gray-200">
        <% @products.each do |product| %>
          <li class="px-4 py-4">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-sm font-medium text-gray-900"><%= product.name %></p>
                <p class="text-sm text-gray-500"><%= product.description %></p>
                <p class="text-xs text-gray-400 mt-1">ID: <%= product.id %></p>
              </div>
              <div class="text-right">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium 
                  <%= product.active ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                  <%= product.active ? 'Ativo' : 'Inativo' %>
                </span>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>

<!-- Setup Instructions -->
<div class="mt-8">
  <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">Configuração do Stripe</h3>
        <div class="mt-2 text-sm text-blue-700">
          <p>Para configurar o Stripe:</p>
          <ol class="list-decimal list-inside mt-2 space-y-1">
            <li>Crie uma conta no <a href="https://stripe.com" target="_blank" class="underline">Stripe</a></li>
            <li>Obtenha suas chaves de API no painel do Stripe</li>
            <li>Configure as variáveis de ambiente ou credentials do Rails:</li>
          </ol>
          <div class="mt-3 p-3 bg-blue-100 rounded text-xs font-mono">
            <p>STRIPE_PUBLISHABLE_KEY=pk_test_...</p>
            <p>STRIPE_SECRET_KEY=sk_test_...</p>
            <p>STRIPE_WEBHOOK_SECRET=whsec_... (opcional)</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
