<!-- app/views/admin/shopee_config/new.html.erb -->
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Header -->
  <div class="mb-8">
    <nav class="flex" aria-label="Breadcrumb">
      <ol class="flex items-center space-x-4">
        <li>
          <%= link_to "Shopee Config", admin_shopee_configs_path, class: "text-gray-400 hover:text-gray-500" %>
        </li>
        <li>
          <div class="flex items-center">
            <svg class="flex-shrink-0 h-5 w-5 text-gray-300" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
            </svg>
            <span class="ml-4 text-sm font-medium text-gray-500">Nova Configuração</span>
          </div>
        </li>
      </ol>
    </nav>
    <h2 class="mt-2 text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
      Nova Configuração Shopee
    </h2>
    <p class="mt-1 text-sm text-gray-500">
      Configure as credenciais master para integração centralizada com a API Shopee
    </p>
  </div>

  <!-- Info Card -->
  <div class="mb-8 bg-blue-50 border border-blue-200 rounded-lg p-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">
          Configuração Master da API Shopee
        </h3>
        <div class="mt-2 text-sm text-blue-700">
          <p>Esta configuração permite que o LinkFlow use suas credenciais master para autenticar usuários finais, simplificando o processo de integração.</p>
          <ul class="mt-2 list-disc list-inside space-y-1">
            <li>Usuários não precisam criar aplicativos Shopee individuais</li>
            <li>Autenticação centralizada usando suas credenciais master</li>
            <li>Melhor controle e monitoramento de rate limits</li>
            <li>Suporte a múltiplos mercados (BR, MX, CO, CL)</li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Form -->
  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <%= form_with model: [:admin, @config], local: true, html: { class: "space-y-6" } do |form| %>
        <% if @config.errors.any? %>
          <div class="rounded-md bg-red-50 p-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">
                  <%= pluralize(@config.errors.count, "erro foi", "erros foram") %> encontrado<%= 's' if @config.errors.count > 1 %>:
                </h3>
                <div class="mt-2 text-sm text-red-700">
                  <ul class="list-disc list-inside space-y-1">
                    <% @config.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <!-- Market Selection -->
        <div>
          <%= form.label :market, "Mercado", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= form.select :market, 
                options_for_select([
                  ['Brasil', 'BR'],
                  ['México', 'MX'], 
                  ['Colômbia', 'CO'],
                  ['Chile', 'CL']
                ], params[:market] || @config.market),
                { prompt: 'Selecione o mercado' },
                { class: "block w-full pl-3 pr-10 py-2 text-base border-gray-300 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm rounded-md" } %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            Escolha o mercado Shopee para esta configuração
          </p>
        </div>

        <!-- Endpoint (Auto-filled based on market) -->
        <div>
          <%= form.label :endpoint, "Endpoint da API", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= form.text_field :endpoint, 
                placeholder: "https://open-api.affiliate.shopee.com.br/graphql",
                class: "block w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            URL do endpoint GraphQL da API Shopee para o mercado selecionado
          </p>
        </div>

        <!-- Master App ID -->
        <div>
          <%= form.label :master_app_id, "App ID Master", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= form.text_field :master_app_id, 
                placeholder: "Seu App ID master da Shopee",
                class: "block w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            O App ID da sua aplicação master registrada na Shopee
          </p>
        </div>

        <!-- Master App Secret -->
        <div>
          <%= form.label :master_app_secret, "App Secret Master", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= form.password_field :master_app_secret, 
                placeholder: "Seu App Secret master da Shopee",
                class: "block w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            O App Secret da sua aplicação master (será criptografado automaticamente)
          </p>
        </div>

        <!-- Active Toggle -->
        <div class="flex items-center">
          <%= form.check_box :active, 
              class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
          <%= form.label :active, "Ativar configuração", class: "ml-2 block text-sm text-gray-900" %>
          <p class="ml-2 text-sm text-gray-500">(pode ser alterado depois)</p>
        </div>

        <!-- Action Buttons -->
        <div class="flex justify-end space-x-3">
          <%= link_to "Cancelar", admin_shopee_configs_path, 
              class: "bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
          <%= form.submit "Criar Configuração", 
              class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Getting Credentials Help -->
  <div class="mt-8 bg-gray-50 border border-gray-200 rounded-lg p-6">
    <h3 class="text-lg font-medium text-gray-900 mb-4">Como obter as credenciais master?</h3>
    <div class="prose text-sm text-gray-700 max-w-none">
      <ol class="list-decimal list-inside space-y-2">
        <li>Acesse o <a href="https://open.shopee.com/" target="_blank" class="text-indigo-600 hover:text-indigo-500">Shopee Open Platform</a></li>
        <li>Faça login com sua conta Shopee</li>
        <li>Vá para "My Apps" e crie uma nova aplicação</li>
        <li>Selecione "Affiliate API" como tipo de aplicação</li>
        <li>Configure sua aplicação com as informações da LinkFlow</li>
        <li>Após aprovação, copie o App ID e App Secret</li>
        <li>Cole as credenciais nos campos acima</li>
      </ol>
      <div class="mt-4 p-3 bg-yellow-50 border border-yellow-200 rounded">
        <p><strong>Importante:</strong> Mantenha suas credenciais master seguras. Elas serão criptografadas e usadas apenas para autenticar usuários finais através da LinkFlow.</p>
      </div>
    </div>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const marketSelect = document.querySelector('#shopee_master_config_market');
  const endpointField = document.querySelector('#shopee_master_config_endpoint');
  
  const endpoints = {
    'BR': 'https://open-api.affiliate.shopee.com.br/graphql',
    'MX': 'https://open-api.affiliate.shopee.com.mx/graphql',
    'CO': 'https://open-api.affiliate.shopee.com.co/graphql',
    'CL': 'https://open-api.affiliate.shopee.cl/graphql'
  };
  
  marketSelect.addEventListener('change', function() {
    const selectedMarket = this.value;
    if (endpoints[selectedMarket]) {
      endpointField.value = endpoints[selectedMarket];
    }
  });
  
  // Set initial value if market is pre-selected
  if (marketSelect.value && endpoints[marketSelect.value]) {
    endpointField.value = endpoints[marketSelect.value];
  }
});
</script>
