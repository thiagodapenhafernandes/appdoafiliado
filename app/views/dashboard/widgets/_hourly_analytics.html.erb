<%= plan_access_overlay('pro') do %>
  <div class="bg-white shadow rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-lg leading-6 font-medium text-gray-900">📊 Análises de Horário</h3>
        <div class="flex items-center space-x-2">
          <span class="text-sm text-gray-500">Cliques únicos</span>
          <button class="text-gray-400 hover:text-gray-600">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Layout melhorado dos dados horários -->
      <div class="space-y-4">
        <% 
          # Obter dados reais de cliques por hora do banco de dados
          # Se não houver dados reais, usar dados simulados baseados em padrões típicos
          if current_user.website_clicks.any?
            # Dados reais do banco
            hourly_data = current_user.website_clicks
              .where(click_time: 30.days.ago..Time.current)
              .group("EXTRACT(HOUR FROM click_time)")
              .count
            total_real_clicks = hourly_data.values.sum
            using_real_data = true
          else
            # Dados simulados mais realísticos se não houver dados reais
            hourly_data = {}
            total_real_clicks = 0
            using_real_data = false
          end
          
          # Primeiro, vamos calcular todos os dados horários (reais ou simulados)
          complete_hourly_data = {}
          (0..23).each do |hour|
            if using_real_data
              # Usar dados reais se disponíveis
              complete_hourly_data[hour] = hourly_data[hour.to_f] || hourly_data[hour.to_s] || 0
            else
              # Simular dados baseado em padrões típicos
              complete_hourly_data[hour] = case hour
              when 0..5   then [0, 1, 2].sample  # Madrugada - muito baixa
              when 6..8   then [1, 2, 3].sample  # Manhã cedo - baixa
              when 9..11  then [3, 4, 5, 6].sample  # Manhã - média
              when 12..14 then [5, 6, 7, 8, 9].sample  # Almoço/tarde - alta
              when 15..17 then [7, 8, 9, 10].sample  # Tarde - pico
              when 18..20 then [4, 5, 6, 7, 8].sample  # Início noite - média-alta
              when 21..23 then [2, 3, 4, 5].sample  # Noite - baixa
              end
            end
          end
          
          # Agora calcular os períodos baseado nos dados completos
          periods = [
            { 
              name: "Madrugada", 
              hours: "00h-05h", 
              color: "bg-blue-500", 
              range: (0..5),
              clicks: (0..5).sum { |hour| complete_hourly_data[hour] || 0 }
            },
            { 
              name: "Manhã", 
              hours: "06h-11h", 
              color: "bg-green-500", 
              range: (6..11),
              clicks: (6..11).sum { |hour| complete_hourly_data[hour] || 0 }
            },
            { 
              name: "Tarde", 
              hours: "12h-17h", 
              color: "bg-orange-500", 
              range: (12..17),
              clicks: (12..17).sum { |hour| complete_hourly_data[hour] || 0 }
            },
            { 
              name: "Noite", 
              hours: "18h-23h", 
              color: "bg-purple-500", 
              range: (18..23),
              clicks: (18..23).sum { |hour| complete_hourly_data[hour] || 0 }
            }
          ]
          
          # Recalcular total para dados simulados
          total_displayed_clicks = complete_hourly_data.values.sum
        %>
        
        <!-- Períodos do dia -->
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-3 mb-4">
          <% periods.each do |period| %>
            <div class="text-center p-3 rounded-lg border border-gray-200 hover:shadow-md transition-shadow">
              <div class="<%= period[:color] %> w-full h-3 rounded-full mb-2 opacity-80"></div>
              <div class="text-sm font-medium text-gray-900"><%= period[:name] %></div>
              <div class="text-xs text-gray-500"><%= period[:hours] %></div>
              <div class="text-lg font-bold text-gray-800 mt-1"><%= period[:clicks] %></div>
              <div class="text-xs text-gray-400">cliques</div>
            </div>
          <% end %>
        </div>

        <!-- Heatmap horizontal responsivo -->
        <div class="bg-gray-50 p-4 rounded-lg">
          <h4 class="text-sm font-medium text-gray-700 mb-3">Distribuição por Hora</h4>
          
          <!-- Grid responsivo de 24 horas -->
          <div class="flex flex-wrap gap-1 sm:gap-2">
            <% (0..23).each do |hour| %>
              <% 
                # Usar os dados já calculados no complete_hourly_data
                clicks = complete_hourly_data[hour] || 0
                
                # Determinar cor baseada na intensidade
                max_clicks = complete_hourly_data.values.max || 10
                intensity_percent = max_clicks > 0 ? (clicks.to_f / max_clicks * 100) : 0
                
                color_class = case intensity_percent
                when 0..10   then "bg-gray-200 text-gray-600"
                when 11..25  then "bg-blue-300 text-blue-800"
                when 26..50  then "bg-green-400 text-green-800"
                when 51..75  then "bg-yellow-500 text-yellow-900"
                when 76..90  then "bg-orange-500 text-white"
                else "bg-red-500 text-white"
                end
              %>
              <div class="relative group flex-1 min-w-[2.5rem] max-w-[4rem]">
                <div class="<%= color_class %> h-12 sm:h-14 lg:h-16 rounded-md flex flex-col items-center justify-center text-xs font-medium transition-transform hover:scale-105 cursor-pointer shadow-sm w-full">
                  <!-- Hora (responsiva) -->
                  <div class="text-xs font-bold hidden sm:block"><%= sprintf("%02d", hour) %>h</div>
                  <div class="text-xs font-bold sm:hidden"><%= hour %></div>
                  <!-- Número de cliques -->
                  <div class="text-sm sm:text-lg font-bold"><%= clicks %></div>
                </div>
                
                <!-- Tooltip melhorado -->
                <div class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none z-10">
                  <div class="bg-gray-900 text-white text-xs rounded-lg py-2 px-3 whitespace-nowrap">
                    <div class="font-semibold"><%= sprintf("%02d:00 - %02d:59", hour, hour) %></div>
                    <div><%= clicks %> cliques únicos</div>
                    <div class="text-gray-300">
                      <%= case hour
                      when 0..5   then "😴 Madrugada"
                      when 6..11  then "🌅 Manhã"
                      when 12..17 then "☀️ Tarde"
                      else "🌙 Noite"
                      end %>
                    </div>
                  </div>
                  <!-- Seta do tooltip -->
                  <div class="absolute top-full left-1/2 transform -translate-x-1/2 border-4 border-transparent border-t-gray-900"></div>
                </div>
              </div>
            <% end %>
          </div>
          
          <!-- Legenda responsiva -->
          <div class="mt-3 text-center">
            <p class="text-xs text-gray-500">
              <%= using_real_data ? "📊 Dados reais dos últimos 30 dias (#{total_displayed_clicks} cliques)" : "📋 Dados simulados - conecte suas análises para ver dados reais (#{total_displayed_clicks} cliques de exemplo)" %>
            </p>
          </div>
        </div>

        <!-- Insights automáticos -->
        <div class="bg-blue-50 p-4 rounded-lg">
          <div class="flex items-start space-x-3">
            <div class="flex-shrink-0">
              <svg class="w-5 h-5 text-blue-500 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="text-sm font-medium text-blue-900">💡 Insights Automáticos</h4>
              <div class="mt-2 text-sm text-blue-800 space-y-1">
                <% if using_real_data && total_real_clicks > 0 %>
                  <% peak_hour = complete_hourly_data.max_by { |hour, clicks| clicks } %>
                  <% if peak_hour %>
                    <div>🔥 <strong>Horário de pico:</strong> <%= sprintf("%02d:00", peak_hour[0].to_i) %> (<%= peak_hour[1] %> cliques)</div>
                  <% end %>
                  <% afternoon_clicks = periods[2][:clicks] %>
                  <% if afternoon_clicks > 0 %>
                    <div>☀️ <strong>Tarde ativa:</strong> <%= afternoon_clicks %> cliques entre 12h-17h</div>
                  <% end %>
                  <div>📊 <strong>Total analisado:</strong> <%= total_real_clicks %> cliques únicos</div>
                <% else %>
                  <% peak_hour = complete_hourly_data.max_by { |hour, clicks| clicks } %>
                  <% if peak_hour %>
                    <div>🔥 <strong>Horário de pico típico:</strong> <%= sprintf("%02d:00", peak_hour[0].to_i) %> (<%= peak_hour[1] %> cliques)</div>
                  <% end %>
                  <% afternoon_clicks = periods[2][:clicks] %>
                  <% if afternoon_clicks > 0 %>
                    <div>☀️ <strong>Tarde ativa:</strong> <%= afternoon_clicks %> cliques entre 12h-17h</div>
                  <% end %>
                  <div>� <strong>Total simulado:</strong> <%= total_displayed_clicks %> cliques de exemplo</div>
                <% end %>
                <div>📊 <strong>Recomendação:</strong> Foque campanhas entre 12h-18h</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
