<div class="min-h-screen bg-gray-50 p-6">
  <div class="max-w-7xl mx-auto">
    <% if @advanced_tracking_locked %>
      <div class="space-y-6">
        <%= render 'shared/plan_gate',
            title: 'Disponível apenas no Plano Completo',
            message: 'Importe o CSV oficial da Shopee para acompanhar origens, horários e regiões com dados atualizados em tempo real. Assine o Plano Completo para desbloquear.',
            cta_label: 'Ver Plano Completo' %>
        <div class="bg-white border border-gray-200 rounded-2xl p-6 shadow-sm">
          <h2 class="text-lg font-semibold text-gray-900 mb-3">O que você desbloqueia</h2>
          <ul class="list-disc list-inside text-sm text-gray-600 space-y-1">
            <li>Importação ilimitada com limpeza automática dos dados da Shopee</li>
            <li>Dashboards avançados por origem, região, hora do dia e subID</li>
            <li>Alertas de picos de performance para otimizar campanhas rapidamente</li>
          </ul>
        </div>
      </div>
    <% else %>
      
          <!-- Filtros e Relatório Detalhado -->
          <div class="bg-white rounded-lg shadow-sm border border-gray-200 mb-8 p-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">Relatório de cliques</h2>
            <div class="grid grid-cols-1 md:grid-cols-5 gap-4 mb-4">
              <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Data Inicial</label>
                <input type="date" class="w-full border-gray-300 rounded-lg text-sm px-3 py-2" name="start_date" value="<%= params[:start_date] %>">
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Data Final</label>
                <input type="date" class="w-full border-gray-300 rounded-lg text-sm px-3 py-2" name="end_date" value="<%= params[:end_date] %>">
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Sub_id</label>
                <input type="text" class="w-full border-gray-300 rounded-lg text-sm px-3 py-2" name="sub_id" placeholder="Pesquisar por sub_id" value="<%= params[:sub_id] %>">
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">ID dos Cliques</label>
                <input type="text" class="w-full border-gray-300 rounded-lg text-sm px-3 py-2" name="click_id" placeholder="Pesquisar por ID de clique" value="<%= params[:click_id] %>">
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Região dos Cliques</label>
                <input type="text" class="w-full border-gray-300 rounded-lg text-sm px-3 py-2" name="region" placeholder="Pesquisar por região" value="<%= params[:region] %>">
              </div>
            </div>
            <div class="flex flex-wrap gap-2 mb-4">
              <button class="bg-primary text-white px-4 py-2 rounded-lg font-medium hover:bg-primary/90 transition">Pesquisar</button>
              <button class="bg-gray-200 text-gray-700 px-4 py-2 rounded-lg font-medium hover:bg-gray-300 transition">Limpar</button>
              <button class="bg-accent text-white px-4 py-2 rounded-lg font-medium hover:bg-accent/90 transition">Exportar</button>
              <button class="bg-red-100 text-red-700 px-4 py-2 rounded-lg font-medium hover:bg-red-200 transition">Deletar Período</button>
              <button class="bg-red-600 text-white px-4 py-2 rounded-lg font-medium hover:bg-red-700 transition">Deletar TODOS</button>
            </div>
            <div class="overflow-x-auto">
              <table class="min-w-full text-xs">
                <thead>
                  <tr class="border-b border-gray-200 bg-gray-50">
                    <th class="py-2 px-3 text-left font-semibold text-gray-700">ID dos Cliques</th>
                    <th class="py-2 px-3 text-left font-semibold text-gray-700">Período dos Cliques</th>
                    <th class="py-2 px-3 text-left font-semibold text-gray-700">Região dos Cliques</th>
                    <th class="py-2 px-3 text-left font-semibold text-gray-700">Sub_id</th>
                    <th class="py-2 px-3 text-left font-semibold text-gray-700">Referenciador</th>
                  </tr>
                </thead>
                <tbody>
                  <% (@csv_data || @clicks || []).each do |click| %>
                    <tr class="border-b border-gray-100">
                      <td class="py-2 px-3 text-gray-900"><%= click[:click_id] || click.click_id %></td>
                      <td class="py-2 px-3 text-gray-900"><%= (click[:click_time] || click.click_time).try(:strftime, '%d/%m/%Y %H:%M:%S') %></td>
                      <td class="py-2 px-3 text-gray-900"><%= click[:region] || click.region %></td>
                      <td class="py-2 px-3 text-gray-900"><%= click[:sub_id] || click.sub_id %></td>
                      <td class="py-2 px-3 text-gray-900"><%= click[:referrer] || click.referrer %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>

      <div class="bg-white rounded-lg shadow p-6 mb-8">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-6">
          <div>
            <h3 class="text-lg font-semibold text-gray-900">Resumo Geral</h3>
            <p class="text-sm text-gray-500">Visão rápida dos seus cliques nos últimos períodos</p>
          </div>
          <div class="flex flex-wrap gap-2 mt-4 md:mt-0">
            <%= link_to clicks_analytics_path(file_id: params[:file_id], from_csv: true), class: "bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition-colors flex items-center" do %>
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"></path>
              </svg>
              Ver Dados do Banco
            <% end %>
            <% if params[:from_csv] == 'true' %>
              <%= button_to 'Persistir no Banco', persist_clicks_csv_to_db_path(file_id: params[:file_id]), method: :post, class: 'bg-blue-700 hover:bg-blue-800 text-white font-bold py-2 px-4 rounded transition-colors flex items-center', data: { confirm: 'Deseja realmente importar e salvar esses cliques no banco?' } %>
            <% else %>
              <%= link_to clicks_analytics_import_csv_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition-colors flex items-center" do %>
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                </svg>
                Importar CSV
              <% end %>
            <% end %>
            <%= link_to 'Ver Importações', clicks_analytics_imports_path, class: 'bg-purple-600 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded transition-colors flex items-center' %>
          </div>
        </div>

        <!-- Filtro de Data -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg p-4 mb-6">
          <form method="get" action="<%= clicks_analytics_path %>" class="flex flex-wrap items-end gap-4">
            <%# Mantém o parâmetro from_csv na URL ao filtrar %>
            <% if params[:from_csv] %>
              <input type="hidden" name="from_csv" value="true">
              <% if params[:file_id] %>
                <input type="hidden" name="file_id" value="<%= params[:file_id] %>">
              <% end %>
            <% end %>
            <div class="flex flex-col">
              <label for="start_date" class="text-sm font-medium text-gray-700 mb-1">Data Inicial</label>
              <input type="date" id="start_date" name="start_date" value="<%= params[:start_date] %>" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex flex-col">
              <label for="end_date" class="text-sm font-medium text-gray-700 mb-1">Data Final</label>
              <input type="date" id="end_date" name="end_date" value="<%= params[:end_date] %>" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex flex-col">
              <label for="sub_id" class="text-sm font-medium text-gray-700 mb-1">Sub_id</label>
              <input type="text" id="sub_id" name="sub_id" value="<%= params[:sub_id] %>" placeholder="Pesquisar por sub_id" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex flex-col">
              <label for="click_id" class="text-sm font-medium text-gray-700 mb-1">ID dos Cliques</label>
              <input type="text" id="click_id" name="click_id" value="<%= params[:click_id] %>" placeholder="Pesquisar por ID de clique" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex flex-col">
              <label for="region" class="text-sm font-medium text-gray-700 mb-1">Região dos Cliques</label>
              <input type="text" id="region" name="region" value="<%= params[:region] %>" placeholder="Pesquisar por região de clique" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex flex-col">
              <label for="referrer" class="text-sm font-medium text-gray-700 mb-1">Referenciador</label>
              <input type="text" id="referrer" name="referrer" value="<%= params[:referrer] %>" placeholder="Pesquisar por referenciador" class="border border-gray-300 rounded-md px-3 py-2 text-sm">
            </div>
            <div class="flex gap-2 mt-6">
              <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors flex items-center">Pesquisar</button>
              <%= link_to clicks_analytics_path(file_id: params[:file_id], from_csv: true), class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors flex items-center" do %>Limpar<% end %>
              <%= link_to '#', class: "bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors flex items-center", onclick: "window.print(); return false;" do %>Exportar<% end %>
            </div>
          </form>
        </div>

        <% if @from_csv && @filtered_csv_data %>
          <div class="bg-white rounded-lg shadow p-4 mb-8">
            <h2 class="text-lg font-semibold mb-4">Relatório de cliques</h2>
            <div class="mb-2 text-sm text-gray-700">
              Total de registros: <%= @filtered_csv_data.try(:total_entries) || @filtered_csv_data.size %>
            </div>
            <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">ID dos Cliques</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Período dos Cliques</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Região dos Cliques</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Sub_id</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Referenciador</th>
                  </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-100">
                  <% @filtered_csv_data.each do |click| %>
                    <tr>
                      <td class="px-4 py-2 text-sm text-gray-900"><%= click[:click_id] %></td>
                      <td class="px-4 py-2 text-sm text-gray-900"><%= click[:click_time].strftime('%d/%m/%Y %H:%M:%S') %></td>
                      <td class="px-4 py-2 text-sm text-gray-900"><%= click[:region] %></td>
                      <td class="px-4 py-2 text-sm text-gray-900"><%= click[:sub_id] %></td>
                      <td class="px-4 py-2 text-sm text-gray-900"><%= click[:referrer] %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        <% end %>
      </div>

          <!-- KPI Cards -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <!-- Total Clicks -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium">Total de Cliques</p>
                  <p class="text-3xl font-bold text-gray-900"><%= number_with_delimiter(@total_clicks) %></p>
                </div>
                <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 15l-2 5L9 9l11 4-5 2zm0 0l5 5M7.188 2.239l.777 2.897M5.136 7.965l-2.898-.777M13.95 4.05l-2.122 2.122m-5.657 5.656l-2.122 2.122"></path>
                  </svg>
                </div>
              </div>
            </div>

            <!-- Principal Fonte -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium">Principal Fonte</p>
                  <p class="text-lg font-bold text-gray-900"><%= @top_referrer ? @top_referrer[0] : 'N/A' %></p>
                  <p class="text-sm text-gray-500"><%= @top_referrer ? "#{@top_referrer[1]} cliques" : '' %></p>
                </div>
                <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                  </svg>
                </div>
              </div>
            </div>

            <!-- Principal Região -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium">Principal Região</p>
                  <p class="text-lg font-bold text-gray-900"><%= @top_region ? @top_region[0] : 'N/A' %></p>
                  <p class="text-sm text-gray-500"><%= @top_region ? "#{@top_region[1]} cliques" : '' %></p>
                </div>
                <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </div>
              </div>
            </div>

            <!-- Horário de Pico -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium">Horário de Pico</p>
                  <p class="text-2xl font-bold text-gray-900"><%= @peak_hour || 'N/A' %></p>
                </div>
                <div class="w-12 h-12 bg-orange-100 rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                </div>
              </div>
            </div>
          </div>

          <!-- Charts Row 1 -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
            <!-- Cliques por Dia -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Evolução Diária</h3>
              <div class="h-64">
                <canvas id="dailyClicksChart"></canvas>
              </div>
            </div>

            <!-- Cliques por Hora -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Cliques por Hora</h3>
              <div class="h-64">
                <canvas id="hourlyClicksChart"></canvas>
              </div>
            </div>
          </div>

          <!-- Charts Row 2 -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
            <!-- Cliques por Fonte -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Cliques por Fonte</h3>
              <div class="h-64">
                <canvas id="referrerChart"></canvas>
              </div>
            </div>

            <!-- Cliques por Região -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Cliques por Região</h3>
              <div class="h-64">
                <canvas id="regionChart"></canvas>
              </div>
            </div>
          </div>

          <!-- Detailed Tables -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
            <!-- Referrers Table -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Desempenho por Fonte</h3>
              <div class="overflow-x-auto">
                <table class="min-w-full">
                  <thead>
                    <tr class="border-b border-gray-200">
                      <th class="text-left py-3 px-4 font-medium text-gray-900">Fonte</th>
                      <th class="text-right py-3 px-4 font-medium text-gray-900">Cliques</th>
                      <th class="text-right py-3 px-4 font-medium text-gray-900">%</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% @clicks_by_referrer.sort_by { |_, v| -v }.each do |referrer, clicks| %>
                      <tr class="border-b border-gray-100">
                        <td class="py-3 px-4 text-gray-900"><%= referrer %></td>
                        <td class="py-3 px-4 text-right text-gray-900"><%= number_with_delimiter(clicks) %></td>
                        <td class="py-3 px-4 text-right text-gray-600">
                          <%= number_to_percentage((clicks.to_f / @total_clicks * 100), precision: 1) %>
                        </td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              </div>
            </div>

            <!-- Regions Table -->
            <div class="bg-white rounded-lg p-6 shadow-sm border border-gray-200">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Desempenho por Região</h3>
              <div class="overflow-x-auto">
                <table class="min-w-full">
                  <thead>
                    <tr class="border-b border-gray-200">
                      <th class="text-left py-3 px-4 font-medium text-gray-900">Região</th>
                      <th class="text-right py-3 px-4 font-medium text-gray-900">Cliques</th>
                      <th class="text-right py-3 px-4 font-medium text-gray-900">%</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% @clicks_by_region.sort_by { |_, v| -v }.each do |region, clicks| %>
                      <tr class="border-b border-gray-100">
                        <td class="py-3 px-4 text-gray-900"><%= region %></td>
                        <td class="py-3 px-4 text-right text-gray-900"><%= number_with_delimiter(clicks) %></td>
                        <td class="py-3 px-4 text-right text-gray-600">
                          <%= number_to_percentage((clicks.to_f / @total_clicks * 100), precision: 1) %>
                        </td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Chart.js Script -->
      <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
      <script>
        // Daily Clicks Chart
        const dailyCtx = document.getElementById('dailyClicksChart').getContext('2d');
        new Chart(dailyCtx, {
          type: 'line',
          data: <%= raw @daily_chart_data.to_json %>,
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: {
                display: false
              }
            },
            scales: {
              y: {
                beginAtZero: true,
                ticks: {
                  stepSize: 1
                }
              }
            }
          }
        });

        // Hourly Clicks Chart
        const hourlyCtx = document.getElementById('hourlyClicksChart').getContext('2d');
        new Chart(hourlyCtx, {
          type: 'line',
          data: <%= raw @hourly_chart_data.to_json %>,
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: {
                display: false
              }
            },
            scales: {
              y: {
                beginAtZero: true,
                ticks: {
                  stepSize: 1
                }
              }
            }
          }
        });

        // Referrer Chart
        const referrerCtx = document.getElementById('referrerChart').getContext('2d');
        new Chart(referrerCtx, {
          type: 'bar',
          data: <%= raw @referrer_chart_data.to_json %>,
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: { display: false }
            },
            scales: {
              x: { beginAtZero: true },
              y: { beginAtZero: true }
            }
          }
        });

        // Region Chart
        const regionCtx = document.getElementById('regionChart').getContext('2d');
        new Chart(regionCtx, {
          type: 'bar',
          data: <%= raw @region_chart_data.to_json %>,
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: { display: false }
            },
            scales: {
              x: { beginAtZero: true },
              y: { beginAtZero: true }
            }
          }
        });
      </script>
    <% end %>
  </div>
</div>
